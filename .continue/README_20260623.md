# AREA81 / Samirhv Blog — Estado e Pendências

> Snapshot gerado em **23/06/2026**. Registra o estado atual e o que está pendente. Em snapshots futuros, crie um novo `README_AAAAMMDD.md` nesta pasta.

## O que é

Blog pessoal em **Laravel (PHP 8.4+)**, tema Canvas 7 (Blade), MariaDB. App na subpasta `samirhv/`. Objetivo declarado (`docs/PROJETO.md`): servir de superfície de publicação para um futuro sistema de **resumo automático de notícias por IA** (LLM local via Ollama/Piper, integrado à API `ia.blue3.com.br`).

## Estado do git

- Branch `master`, **sincronizada** com `origin/master` (0 à frente / 0 atrás).
- **Working tree limpa** — nada para commitar. Só existe a branch `master`.
- Último commit: `ae85fc7 "Expl"` — mensagem fora do padrão `versão - comentário` exigido em `CLAUDE.md`.
- Versão atual: **0.1.0** (`version.md`).

## Pendências

### 1. News Digest IA — pipeline central praticamente todo pendente (PRIORIDADE)
Roteiro: `docs/ORGANIZANDO/news-digest-roteiro.md` + `news-digest-projeto.md`. **Todas as caixas `- [ ]`**.
- **Já existe (Fase 0):** `config/news.php`, migrations `news_articles`/`news_digests`, models `NewsArticle`/`NewsDigest`.
- **Falta tudo de Fase 1 a 8** (confirmado ausente no código):
  - `OllamaClient` (Fase 1), `FeedFetcher` (Fase 2), `ArticleEmbedder`+`InterestRanker` (Fase 3), `Summarizer` (Fase 4), `SpeechSynthesizer`/Piper TTS (Fase 5), `BlogPublisher`/`TelegramPublisher`/`DiscordPublisher` (Fase 6), comando `news:digest` + scheduler (Fase 7). Não existe pasta `app/Console`; `routes/console.php` só tem `inspire`.
- **Decisões em aberto** (`news-digest-projeto.md:150-162`): store vetorial pgvector vs MariaDB 11.8 nativo; módulo dentro do blog vs serviço separado; lista de feeds RSS + keywords; frequência de execução.

### 2. Blog — migração de array estático para banco (em transição)
Roteiro `img/blog-roteiro.md` (caixas `- [ ]`, **desatualizado**: o CRUD admin já foi feito no commit `ccc4593` mas as caixas não foram marcadas).
- Bug conhecido (`img/blog-roteiro.md:21`): **"Tags existem no array de cada post, mas não filtram nada ainda."**
- Pendentes: filtro real por tag (`?tag=`), páginas por tag, contadores (`:66-68`); `reading_time` automático, Open Graph/meta, `sitemap.xml`, feed RSS do blog (`:84-87`); integração da seção de digests (rotas `news.index`/`news.show`, player MP3, menu) (`:90-97`).

### 3. Higiene / limpeza
- `docs/CONTINUA/` e `docs/ADMIN/` contêm **duplicatas de código já integrado** na app (newsletter, admin CRUD) — candidatas a remoção.
- Roteiros desatualizados como rastreadores (caixas concluídas não marcadas).
- Último commit com mensagem `"Expl"` fora do padrão de commits.

## Sem TODO/FIXME reais no código da app
Os matches de grep foram falsos positivos ("Pendente" = status de assinante em `NewsletterService.php`; `placeholder=` em views; vendor Filament).

## Próximos passos sugeridos
1. Decidir store vetorial + topologia do News Digest e começar a Fase 1 (`OllamaClient`).
2. Implementar filtro por tag no blog (pendência menor e isolada).
3. Limpar `docs/CONTINUA/` e `docs/ADMIN/`; atualizar/arquivar os roteiros.
